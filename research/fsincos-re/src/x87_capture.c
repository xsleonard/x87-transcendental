/*
 * The capture-kit copy is canonical because it is also the exact source used
 * for the checked-in static retro-machine binaries.  Keep this source-tree
 * entry point as a thin include so the two harnesses cannot drift.
 */
#include "../capture-kit/x87_capture.c"
