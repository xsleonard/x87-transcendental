# H1638–H1643: isolated tiny C and a fresh remaining-scope challenge

2026-09-04. Research evidence only. No production/default or paper/PDF edit.
The full goal remains active/unachieved; finite success is not silicon proof.

## Outcome

H1638 implements the H1637 tiny predecessor rule in the isolated combined C
program. All 109,905 retained tiny output/C1 checks pass on O0/O2/O3/UBSan,
while the tested polynomial/table outputs and metadata remain unchanged.

H1641 then executes 6,432 fresh full tuples on the authorized Skylake Xeon
45.32.204.118, once each, after local public/private freshness checks and an
immutable prediction freeze. H1642 and independent H1643 both pass every
frozen prediction: 6,432 outputs/C2 responses, 4,512 C1 bits and 3,744 exception
masks. Full recorded value/status pairs agree across PC24/PC53/PC64 in all
2,144 instruction/mode/operand groups. No candidate adjustment was made.

H1641 is **OPENED_ONCE**. Never rerun its tuples or its runner, including for
timing, warm-up or selftest. The remote directory is
`/root/fsincos-h1641-remaining-scope`; the local kit is `transfer-tests/h1641`.
The i7 remains 142.132.217.24, not historical .243. No i7 execution occurred
here, and no unrelated remote service was touched. Standing campaign authority
is already granted; no repeat authorization question is necessary.

## H1638: fixed C tiny path

`experiments/h1638_tiny_closed_form.h` defaults `G_H1638_TINY` to zero. The
driver inserts it into an assembled analysis source, alongside the unchanged
H1630 polynomial and H1633 table programs, with R84 OFF. It does not edit the
canonical source. It uses the existing M66 reduction/quadrant route, then
selects the leading magnitude or its 64-bit predecessor by sign/mode. Direct
external exponent below -68 retains its separate bypass and C1=0. Other tiny
results have C1=0 for predecessor, C1=1 for leading. Power-of-two predecessor
crossings are explicit; non-bypass tiny results are normal. The C1 observer
is not a complete architectural-status API.

Every row of the H1637 mixed banks runs through all four builds:

| Path | Retained appearances | Output/C1 result |
| --- | ---: | --- |
| Tiny | 109,905 | All pass; separate integer formula also checked |
| Polynomial | 145,254 | All pass; unchanged metadata |
| Table | 196,245 | All pass; unchanged metadata |
| Out-of-range | 18 | C2 response passes; C1 unscored |
| Total | 451,422 | 1,805,688 compiler-row checks |

A 374-operand software preflight contributes 11,968 compiler-row checks,
including 4,480 tiny checks. Its special encodings are software tests, not
hardware evidence. Four builds also retain all 81 current frontier outputs
and 53 historical outputs/30 known C1 bits, with nine overlapping tuples.
All candidate build selftests pass. No second full H1638 artifact-directory
replay is claimed; its complete banks were checked with four builds and the
separate tiny formula in the original run.

## H1639: exact centers and predictions before observation

The software proposal bank contains 358 unique external operands, 2,864
instruction/mode predictions and 8,592 proposed PC-expanded full tuples.
All numerical predictions agree across four C builds and the separate
rational/integer specification. Unknown special C1/exception fields remain
null. Normal nonzero finite in-range exception mask 0x20 is a separately
declared hypothesis, not inferred from final-rounding exactness.

Exact table-center preimages are enumerated across every valid external
reduction exponent e=-1..62. For center b/64, residual sign fixes
`D = +/- b*2^59`; write `A = sig*2^(e+2) = N*M66 + D`. Since M66 is odd,
`N = -D*M66^-1 mod 2^(e+2)`. Intersect that congruence class with the complete
normalized 64-bit significand interval. Its N interval is shorter than the
modulus, so at most one candidate occurs per exponent/center/residual sign.
The code verifies the exact nearest quotient and excludes unreduced inputs.

There are eight positive-external reduced aliases, sixteen with negative
external mirrors, for the six reachable zero-offset centers b=18,22,26,30,36,44.
Twelve signed direct centers are also proposed. This is complete for this
specified M66 numerical reducer, not a recovered silicon reduction circuit.
b=52 has no reachable zero offset; b=60 is unreachable. Do not invent either
as a missing zero-offset test. See the bank for the complete integer witnesses.

## H1640–H1641: conservative freshness and the one-shot campaign

H1640 rejects any previously visible 16-hex significand, irrespective of
exponent, sign, instruction, RC, PC or host. This is stricter than full-tuple
freshness and rejects software-only appearances too; rejection does NOT
establish hardware coverage. Compressed histories are searched. Only the
declared software bank and audit output are excluded from public history.
The private ledger stays local; only aggregate counts are published.

Thirty significands have public hits, four have private hits, with union
thirty. The remaining 268 operands include the following disjoint kinds:

| Kind | Operands | Fresh full tuples |
| --- | ---: | ---: |
| Exact table-center preimage | 2 | 48 |
| Table-lane boundary | 82 | 1,968 |
| Tiny-dispatch boundary | 64 | 1,536 |
| Range boundary | 24 | 576 |
| Denormal | 16 | 384 |
| Pseudo-denormal | 16 | 384 |
| Quiet NaN | 16 | 384 |
| Signaling NaN | 16 | 384 |
| Invalid special encoding | 16 | 384 |
| Unnormal | 16 | 384 |
| Total | 268 | 6,432 |

All eligible operands are selected, with both instructions, four rounding
modes and three precision controls. Before freeze, H1641 replays the four C
builds and independent formulas, then repeats the read-only public/private
audit on the selection: zero collisions. The immutable manifest, inputs and
runner are hashed. Private file identities, contents and hashes are absent.

The runner validates all uploaded hashes, the existing capture binary, and
reported GenuineIntel family 6/model 85 before an atomic `hardware-output`
directory creation consumes the campaign. It contains no repetitions, timing
or selftest. All 24 lanes completed, 268 rows each. The capture binary SHA is
`9eef49556c7da32c270b1f1c29f777bfffbba7eb85a68ddb512e8e7e1a192af1`.
The host reports Intel Xeon Processor (Skylake, IBRS), stepping 4, microcode
0x1, with virtualization metadata retained. This is evidence from that host,
not cross-host architectural universality.

## H1642–H1643: scores, new scope and remaining limits

All 6,432 frozen output/C2 predictions pass. Of these, 384 are out-of-range
C2 markers, not retained ST0 payload checks. All 4,512 predicted C1 bits and
3,744 predicted exception masks pass. Null fields receive no correctness
credit. The independent verifier uses separate raw parsing, the rational
table/polynomial graph and integer tiny specification, and compares every
observed output against all four C builds: 25,728 software comparisons.

The newly observed exact-center pair is
`403d:fb53d14aa9c2f2c1` and `c03d:fb53d14aa9c2f2c1`. Its positive external
quotient is 5764607523034234880 and signed residual is -30/64. All 48 output,
C1 and exception predictions pass. This fills one signed high-q zero-offset
center, not all six direct centers or all preimages. The other center cases
were rejected by conservative freshness; they are not silently counted as
observed. This campaign also supplies actual FCOS RZ/PC evidence in its
selected domains, not a universal PC-independence proof.

Previously unpredicted status fields give these descriptive observations:

| Kind/instruction | Raw SW | Observations |
| --- | --- | ---: |
| Denormal FSIN | 3832 | 192 |
| Denormal FCOS | 3822 | 192 |
| Pseudo-denormal, each instruction | 3822 | 192 each |
| Quiet NaN, each instruction | 3800 | 192 each |
| Signaling NaN, each instruction | 3801 | 192 each |
| Invalid special encoding, each instruction | 3801 | 192 each |
| Unnormal, each instruction | 3801 | 192 each |
| Out-of-range C2, each instruction | 3c00 | 192 each |

In particular the selected true denormal FSIN cases record underflow in
addition to denormal/precision; FCOS and pseudo-denormals do not record that
underflow here. These fields were not frozen predictions, and are not promoted
to general special-status laws. Status is sampled after FLD+instruction and
before FSTP. Load effects, arbitrary initial condition codes/TOP, stack faults,
unmasked exceptions and reserved controls remain separate obligations. Zero,
infinity and invalid-zero-payload proposals were not fresh and were not run.

## Anchors and continuation

| Artifact (relative to fsincos-re) | SHA256 |
| --- | --- |
| `tmp/ledger33/current/h1638_tiny_c_transfer/report.json` | `db6c8cbc1e0e2c9acdb54da5e1e381407a6b8c5f6f62cc7b1c52c4d90f8510e0` |
| `tmp/ledger33/current/h1639_remaining_scope_proposals/bank.json` | `c9b28e70b337b3fe3ce2fa75debc8b1c97f5d18168ebc1e1c4eb528bc8d617f1` |
| `tmp/ledger33/current/h1640_remaining_scope_freshness/report.json` | `4e55a5041ba75cca79689e9cfa2173d6c4bffab7be035322f8bae3776e9d3b7c` |
| `transfer-tests/h1641/FREEZE.json` | `066ac4f086d9132ba157195af02a3aaffaccb4f3e05c5337a4689b634f35f833` |
| `transfer-tests/h1641/manifest.json` | `453ecfe5ab81dc49c23cd045d85af341df3e8e6b2125cca6dd7c064568d06a84` |
| `tmp/ledger33/current/h1642_score_remaining_scope/report.json` | `8f19ff7e9a95df22824497abec43b765ee73d832296444bed9976b86b7092293` |
| `tmp/ledger33/current/h1643_independent_remaining_scope/report.json` | `fb4b5a78f98af20ed492b442976379bffc05dd29cf026aa510e2ee3bf6f0eeaf` |

H1639 and H1643 complete output directories reproduce byte-for-byte in a
separate software replay under `/private/tmp/h1639-h1643-root-replay.9KlPSc`.
Python syntax, runner shell syntax, canonical build, both emulator selftests,
tracked diff and new-file whitespace checks pass. The frozen kit checksums
still verify after capture. Its OPENED_ONCE sidecar is also copied to the
same isolated remote directory without altering the immutable freeze.
Canonical source remains SHA256
`0339a7d6161c29164232fadd46053a538b7163d5d4449889e3600e9245026f2b`.
Speculative defaults remain off and incumbent R96 remains empirical/incomplete.
The documented incumbent frontier remains direct 50/48 and external 81/79;
this campaign is not a new comprehensive incumbent-frontier census. No fixed
candidate miss was found. No production promotion or paper/PDF edit occurred.

Next: separate special-value load/instruction/status semantics and reconcile
the remaining center/zero/infinity evidence with exact full-tuple provenance.
Do not weaken the signature policy silently or treat its rejections as
coverage; a deliberate full-tuple reconciliation may establish unused tuples.
Do not fit new selectors or repeat captured tuples. The fixed arithmetic has
survived another prospective test, but full all-input silicon correctness and
complete architectural state remain unproved.
