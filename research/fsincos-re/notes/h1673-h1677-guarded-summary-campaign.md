# H1673–H1677: guarded summary injection collapses before consumption

2026-09-04 local date; capture 2026-09-05 02:58:01–02 UTC. H1675 completed
38,016 fresh tuples once on the authorized Skylake Xeon, with no retries.
Its actual restored snapshots cover only U=ES=B despite all eight requested
states. Identity restoration is falsified on 28,512 rows. The six named
pending rules remain indistinguishable; no selector is promoted. The full
goal is active/unachieved. Paper/PDF and production defaults are unchanged.

Host mapping is the user's: i7 `142.132.217.24`, Skylake Xeon `45.32.204.118`.
The Xeon identifies as GenuineIntel family 6 model 85, stepping 4. No i7
capture, unrelated service mutation, or private ledger transfer occurred.

## Frozen before labels

H1673 uses a new seed, new case identifiers and 792 new significands, not
H1670's held inputs. There are 1,584 signed operands across 11 classes, 18
flag/mask profiles and four requested summary settings. Both instructions,
four rounding modes and PC24/53/64 give 38,016 unique instruction/RC/PC/operand
keys. Four numerical builds agree over 50,688 software evaluations; H1677's
independent rational/integer algebra agrees on all 38,016 conditional
projections, representing 12,672 distinct arithmetic points.

H1674's 62-field parser explicitly distinguishes ATTEMPTED=0. Its preflight
checks 38,016 synthetic positives and before-state mutations, 22,176 output
and C0 mutations each, 15,840 before-only non-credit and empty-snapshot
mutation cases each, 27,456 restoration/delivery/guard combinations, 14,784
unknown-summary non-credit cases, and three malformed-record rejections.
Before-only records earn no endpoint, transition or pending-gate credit.

H1675 replays the bank and preflight, then searches compressed public and
local private history. All 792 proposed significands survive, with zero
public and zero private collisions; 26 private files are examined, with only
aggregates published. It pins the source, ELF, primary and independent
scorers and software evidence before hardware. Frozen predictions are never
changed to match the result.

The primary hypotheses remain identity restoration and requested-ES pending
delivery. Conditional completed-opcode behavior is a separate hypothesis:
fresh arithmetic/exception behavior plus sticky prior flags, with ES/B and
post-execution delivery unpredicted. Selecting the observed fault/completion
branch for conditional scoring is not an unconditional delivery prediction.

## One guarded observation per fresh tuple

`transfer-tests/h1675` and `/root/fsincos-h1675-guarded-summary` are now
OPENED_ONCE, with matching OPENED.json hashes. Never rerun this campaign,
including a partial recovery or smoke test. The atomic hardware-output
directory and runner reject a second invocation. The run used the pinned
H1672 binary; its guard evaluates actual restored state before the opcode.

H1670 remains FROZEN_UNOPENED ON PROTOCOL HOLD. Its original runner must not
be used. H1667 remains unexecuted. The H1672 build directory itself has no
inputs; its binary was executed exactly by the separate H1675 campaign.

All 38,016 H1675 rows have ATTEMPTED=1 because every restored state passed
the safety guard. Zero BEFORE_ONLY records occurred. This does not mean an
unsafe state was consumed: no off-diagonal state survived establishment and
the before snapshot. The preflight's skipped records are synthetic only.

## Requested state is not actual state

Let U = bool(SW & ~CW & 0x3f), ES = SW[7], and B = SW[15]. The observed map is:

| Requested U/ES/B | Actual U/ES/B | Rows |
|---|---|---:|
| 000 | 000 | 5,280 |
| 001 | 000 | 5,280 |
| 010 | 000 | 5,280 |
| 011 | 000 | 5,280 |
| 100 | 111 | 4,224 |
| 101 | 111 | 4,224 |
| 110 | 111 | 4,224 |
| 111 | 111 | 4,224 |

The frozen identity-restoration hypothesis misses 28,512 rows; B-from-ES and
ES-from-U each miss 19,008; both-from-U matches all 38,016. Requested ES
predicts the wrong opcode-fault outcome on 19,008 rows. Identity-based attempt
prediction misses 15,840 rows because states expected to be skipped instead
restore coherently and pass the guard. These falsifications stay in the
primary score; no frozen hypothesis is rewritten.

This localizes collapse only to the state-establishment/observation pipeline.
The experiment does not isolate FXRSTOR, FXSAVE, scheduling or virtualization
internals. It is not a universal FXRSTOR law or proof inconsistent states are
unreachable through every architectural path.

## Conditional results and independent audit

There are 19,392 completed opcodes without a fault, 1,728 completed opcodes
with a subsequent FWAIT fault, and 16,896 pending faults at the opcode with
no completed arithmetic. Thus there are 21,120 completed-opcode endpoint
observations, not 38,016 fresh arithmetic results. All 21,120 frozen endpoints
and known non-summary status projections match; the other 16,896 preserve
their complete before state as specified by the conditional pending branch.
All control, stack, tag and trap checks match, including 18,624 fault-context
relations. The 12,672 PC groups have zero differences in the scored state.
Unknown post-execution ES/B and delivery were not predicted credit.

H1677 independently checks every raw row and derives every frozen projection
without importing H1673/H1674/H1660/H1652. It confirms zero conditional misses.
There is no new numerical miss and no incumbent-frontier census expansion.

H1676 separately enumerates all 256 Boolean functions of actual U/ES/B.
Exactly 64 remain consistent: only f(000)=0 and f(111)=1 are constrained.
For each of the six unobserved actual states, either answer remains possible.
Composing every survivor with the observed requested-to-restored map produces
the same U-controlled outcome for all eight requested states. Consequently
zero misses for the six named actual-state gates is not a six-way selection.
This is an exact finite identifiability result on observed support, not proof
the physical gate has only these three inputs or that the observed restore
map holds universally.

## Continuation boundary

The proposal bank, primary synthetic preflight, primary raw score, independent
raw verification and observability audit reproduce byte-for-byte in
`/private/tmp/h1673-h1677-replay.cy5dXy`. This is software-only replay of
retained evidence, not another freezer or hardware invocation. Python/shell
syntax, production build, both emulator selftests, diff and new-file whitespace
checks pass. All transfer, capture, scoring and replay sessions are terminal.

Do not launch another bank through the same collapsed state path and claim
it identifies the gate. First audit a genuinely distinct state-establishment
and observation path, such as whether a legacy environment restore followed
by an independent no-wait status observation can preserve an off-diagonal
state. This is a proposed investigation, not an established capability.
Any capture must retain the non-retrying trap guard, fresh-tuple audit,
immutable predictions and distinction between requested and actual state.

The numerical candidate remains isolated/default-off. R96 is empirical and
incomplete; the documented incumbent frontier remains direct 50 rows over
48 residuals, external 81 rows over 79 operands. Small normalization shifts
60–63, zero/infinity, reserved controls, complete tag/pointer semantics, exact
centers, other state-establishment paths and all-input silicon equality remain
open. No closure, default or paper/PDF promotion follows from this campaign.

## SHA256 anchors

- H1673 bank: `3a7731ab9a7632bfe8d91c26307a63d8a2e17be3d1fefbd69474ca2991448193`.
- H1674 preflight: `f61dd610f8643111cd2ebe146b84743940b67fbacbdba3fc45e002a999f6a5e4`.
- H1675 freeze: `d1b53fccfa1f5db61aade4cb982ef46ec992b6bd5889797f473710232af1ac1a`.
- H1675 manifest: `8bb285cf30fbedb107dd34edde1b5435017b0e6b2e48f6a292cdab94a45213f5`.
- H1675 raw: `490bded690975f3a50e51e3933a15919f2a54a3808cb29ba13516ea769709a2e`.
- H1675 OPENED: `21a9083a018c97eadc069e7870a814c176d18f6548598b3897d5973d354835f7`.
- H1674 primary report: `08787d5587af4651f4d4d9e80256103583b35fbdf12cc8b4d869baadd4646dfe`.
- H1677 independent report: `801bf03df87b54acd7db1e8f913524d9a96aae2aad499f7339bcf7f206cde061`.
- H1676 observability report: `f5291dbc6b9a0f670c3381b69ca507ec24f590c1f5c345dd6f5ae01d658c6165`.
- H1672 ELF: `72d49ae21259b15c2941a19537976221459a731f5dc6d99d90de0e33e30e7bb6`.
