# H1696–H1697: small-domain generator history and private-format limits

2026-09-04 local date. No hardware capture, manifest freeze, freshness
clearance, candidate/default change or paper/PDF edit. This turn resolves
specific generator questions and strengthens the private representation
check; it does not close the smallest-denormal or zero/infinity obligations.

## H1696: bounded replay and a seed-independent generator exclusion

The pinned H727/H773 mixed generator was executed locally with its file writer
replaced by an in-memory counting/hash sink. Imports are restricted to an
isolated Python RNG and argument object. Only seed, row count and writer name
are substituted. The two generator bodies are AST-identical after these
substitutions and removal of the parameterized copy's extra sys import.
No historical shell runner executes or writes an input/capture file.

| Reported campaign | Seed | Generated operands | Small sig1–15 | Zeros | Infinity appearances |
| --- | --- | ---: | ---: | ---: | ---: |
| randv1 | 0x662662 | 8,000,000 | 0 | 0 | 40,312 |
| rv2/H780 | 0x773001 | 8,000,000 | 0 | 0 | 40,032 |
| rv3 | 0x8623f4 | 2,000,000 | 0 | 0 | 10,054 |
| rv4 | 0x8725 | 2,000,000 | 0 | 0 | 9,999 |
| rv5 | 0x8726 | 2,000,000 | 0 | 0 | 9,959 |
| rv6 | 0x8727 | 2,000,000 | 0 | 0 | 10,134 |

All 24,000,000 rows are parsed and hashed. Their 120,414 true-denormal
appearances contain none of significands1–15. All 17 distinct randv1 indexed
operand anchors in the retained R84 miss table reproduce with one consistent
offset of **zero**. The replay is anchored to source and selected historical
inputs, not a proof of the complete original stream or original Python runtime.
The current runtime is Python3.14.7. Full streams are not saved or transferred.

The infinity count is 120,490 **generated appearances**, not that many distinct
operands or authenticated raw observations. Both signed infinities were already
reserved by H1688. This earns zero numerical/status credit and no recapture
permission. Historical randv1/rv2 reports specify RN/RD/RU; rv3–rv6 specify
all four RC. Original PC/masks/prestates remain unauthenticated.

A separate source/AST check proves the H733 large-argument generator excludes
this entire special domain for **every seed and row count**. Its sole exponent
assignment chooses E in [0x4003,0x403e); the only subsequent E update ORs the
sign bit. Therefore E is never zero or0x7fff. No true/pseudo-denormal, zero,
infinity or NaN can be generated by that pinned program. This is a generator
theorem, not a physical instruction theorem or permission to assume arbitrary
modified generators identical. H939's100M search is not counted as100M captured
inputs: its later filtered-candidate capture remains a separate provenance
question, and H939 was not executed here.

## H1697: the unreserved complement is still not cleared

The public target set is the16 signed raw encodings with E=0 and significands
5,6,9,10,11,12,13,14. This is merely the complement of H1688's historical
reservations inside1–15, not a declaration of eligibility.

The center-only numeric scanner cannot be reused unchanged: E=0 requires the
effective exponent1, giving one denormal ulp of2^-16445, and relevant decimal
scientific exponents are around-4950, outside that scanner's center-domain
cutoff. H1697 uses the correct raw denormal scale and tests exact numeric
equality **and** possible directed rounding onto a target's denormal grid.
It also checks short/padded/packed hex pairs, decimal fields, common raw80/
struct byte layouts, UTF16 views and extracted PDF text. Public synthetic
controls cover all16 encodings and include `2e-4950` and its negative, which
can round to targeted encodings but are not exact target values.

Aggregate-only private result across26 files,6,425,428 bytes and901,694
decoded characters (two PDFs/27 nonempty page texts):

- 18 possible byte-layout occurrences.
- 88 possible paired-hex and82 possible paired-decimal occurrences.
- Zero packed-hex occurrences.
- All11,120 decimal tokens lie outside the target rounding window; no numeric
  token was skipped as unparsed. No hexadecimal-float occurrence was found.

The188 possible occurrences are nonunique syntax/layout matches, **not188
verified tuples**. Incidental tables, metadata and cross-field matches may
account for them, but their semantic roles have not been established. The
status is `POSSIBLE_PRIVATE_COLLISION_UNRESOLVED`; no capture is cleared.
Do not label these matches either confirmed history or harmless false positives
without further local structural evidence. Differences from H1688's broader
counts reflect a smaller target set and changed formats, not a contradiction.

The PDF skill is used only for local read-only text extraction. Private names,
contents, hashes and per-operand membership lists are never printed, serialized
or transferred. Unknown schemas, compressed/encrypted or image-only data and
dynamic generation remain outside this format audit. No PDF was authored,
changed or re-exported; the academic paper remains untouched.

## Archive state and next action

The authorized i7 read-only SSH archive check times out on port22; session39074
ends255. This is terminal, not a live recovery process. A read-only Xeon check
finds none of the four specifically checked old paths, including
`/root/h491/specials`; that is not an exhaustive search of all remote archives.
No remote file is written and no capture starts. Missing raw results never
make an old tuple fresh, and H1688's144 reserved instruction/RC/operand keys
remain in force, with unknown PC reserving all PC.

Next resolve the possible private matches by actual local format/record roles
and finish remaining public generated-history provenance before considering
the16 complement encodings. Bound this work: implementation/formal arithmetic
and reduction/dispatch/state composition remain useful independent directions
that need no new hardware tuple. Do not let archive availability redefine the
full goal or revive same-support summary campaigns. H1685 remains paused,
H1670 held and H1694/all other opened campaigns closed. All81 documented
external incumbent-frontier outputs remain candidate-matched; no new candidate
numerical miss was found. Full all-input silicon/control-state closure remains
unproved. This is concrete provenance progress, not a global blocker.

## Artifact anchors and checks

Paths are relative to `fsincos-re`.

| Artifact | SHA256 |
| --- | --- |
| `experiments/h1696_mixed_generator_small_domain.py` | `6a70c02412400c5c9cbc4667ce21c0c5a0ef18c08e9d334c50227c724fcecc9d` |
| `tmp/ledger33/current/h1696_mixed_generator_small_domain/report.json` | `5e9352997bc3b9aba6b6bf950ed01cc14c57fbc41b89d9865ca53cf7b7a50178` |
| `experiments/h1697_private_small_domain_formats.py` | `63e221edbfe1821099282685875967463e906c595bef8e1576d26b5eac8d8b47` |
| `tmp/ledger33/current/h1697_private_small_domain_formats/report.json` | `9b00b1208edd277ac92781b6793c360d365a65faf444283d7d55767621f82f38` |

The H1696 report pins six per-campaign reports, generated-stream hashes and
public evidence. H1697 records only aggregate private results and public
synthetic controls. Replays use `/private/tmp/h1696-h1697-replay.QKhAP5`;
they reproduce both report directories byte-for-byte and perform software
generation/local format inspection only, no capture.
Python syntax, canonical build and both emulator selftests pass. Production
source0339a7d6, paper802fb3b6 andREADME8096a84f anchors remain unchanged.
