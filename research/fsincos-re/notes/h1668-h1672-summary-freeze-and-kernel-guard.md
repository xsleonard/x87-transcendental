# H1668–H1672: frozen summary hypotheses and a kernel-retry guard

2026-09-04 local date. No new hardware tuples were executed. H1670 freezes
38,016 fresh proposals, then is deliberately held unopened after a kernel
delivery hazard is identified. H1672 builds a separately guarded instrument;
it is also unexecuted. The full goal remains active/unachieved. No numerical
selector, production default or academic paper/PDF changes.

The previous goal turn made progress through the exact H1666 observability
limit and the first summary-state harness. This turn adds predeclared and
independently checked predictions, a conservative freshness freeze, and a
source-backed correction to the instrumentation's no-retry assumptions.

## H1668: distinguish hypotheses from known conditional checks

The bank has1,584 signed operands (792 unique significands),11 encoding/
arithmetic classes,18 flag/mask profiles and all four requested ES/B settings.
Each operand has one prestate and both instructions × four RC × PC24/53/64,
giving38,016 unique old instruction/RC/PC/operand keys. The flags/masks cover
individual exception bits, SF and combined flags; CC and stack depth vary.
Requested (U,ES,B), where U=bool(flags & ~masks &63), covers all eight states:
220 operands each in000/001/010/011 and176 each in100/101/110/111.

The primary hypotheses are identity restoration and ES-controlled pending
delivery. They are not claimed established facts. Conditional on an opcode
actually executing, H1668 freezes its numerical/early-exception result and
status excluding ES/B: H1660 is evaluated without prior pending flags, then
the original flags are ORed back as sticky flags. This projected behavior is
also a hypothesis in the inconsistent domain. It does not silently broaden
H1652, which still rejects inconsistent input state.

Post-execution ES/B and FWAIT delivery remain explicitly unknown. A pending
opcode fault instead predicts unchanged state. Those conditional checks are
scored separately from restoration and pending hypotheses; choosing a path
after seeing actual delivery is not an unconditional delivery prediction.
Four C numerical builds agree on50,688 software evaluations. No endpoint is
chosen by fitting a new hardware label.

## H1669/H1671: preflight before observation

H1669 explicitly validates the61-field H1667 format, adapting only its common
snapshot fields to the pinned older parser. It tests38,016 positive synthetic
rows and detects38,016 output mutations and38,016 C0 mutations. Another38,016
synthetic restoration/delivery combinations exercise the branches independently;
25,344 post-execution summary-bit mutations are correctly left uncredited,
and three malformed records reject.

H1671 imports neither H1668/H1669 nor H1660/H1652. It independently derives
raw class/priority/tag/flags algebra, tiny results, exact Fraction underflow
scaling and the existing rational polynomial/table graph. All38,016 frozen
conditional projections agree over12,672 distinct arithmetic points. These
are software preflight checks, not38,016 observed hardware successes.

## H1670: frozen, unopened, ON HOLD

The freezer reproduces the proposal bank and synthetic checks before scanning
compressed public/private history. All792 candidate significands survive:
zero public and zero private collisions;26 private files are examined with
only aggregates published. Predictions and input order are unchanged. The
immutable kit is `transfer-tests/h1670`; its original checksums still pass.

**Do not execute H1670's original runner.** `HOLD.json` is an additional status
record, not a rewrite of the immutable FREEZE.json. The remote directory
`/root/fsincos-h1670-summary-state` contains only that hold marker: no inputs
were copied there and no hardware-output directory exists. The original
H1667 binary remains unexecuted.

The running host reports6.1.0-52-amd64. Upstream Linux v6.1 classifies x87
faults from SW flags and CW masks; when no code results, its fault handler
returns without SIGFPE and permits retry. Thus an ES/B-induced #MF at U=0
could evade the userspace recovery handler. A timeout limits duration, not
attempt count. This is a conditional risk, not an observed Xeon fault loop;
the exact Debian kernel binary was not disassembled. Sources:
[fault handler](https://github.com/torvalds/linux/blob/v6.1/arch/x86/kernel/traps.c)
and [exception classifier](https://github.com/torvalds/linux/blob/v6.1/arch/x86/kernel/fpu/core.c).
Version-tagged source copies and hashes are retained with H1672's build.

Standing host authorization remains valid; no renewed permission is needed.
The Xeon has no `/dev/kvm` and no detected qemu-system-x86_64 executable, so an
isolated hardware-accelerated guest is not immediately available there. A
read-only i7 SSH attempt times out on port22; it changes no service. Neither
fact is a global goal blocker: guarded observation remains a useful next step.

## H1672: separately guarded, compiled, NOT executed

`capture-kit/x87_guarded_summary_capture.c` derives from immutable H1667 and
adds a guard on the **actually restored** SW/CW:

```text
attempt = bool(actual_SW & ~actual_CW & 0x3f)
          OR (actual_SW & 0x8080) == 0
```

When the guard is false, it performs only non-waiting cleanup after preserving
the before snapshot. Neither FSIN/FCOS nor FWAIT executes. The62-field record
adds ATTEMPTED=0; A_VALID/FAULT/FAULT_AT remain zero and A/F snapshots are empty.
This is BEFORE_ONLY evidence, not instruction-consumption coverage or a passing
pending-gate prediction. Do not send it to the old60/61-field parsers.

GCC12.2.0 compiles it warning-clean in
`/root/fsincos-h1672-guarded-summary`. The static audit verifies both original
six-instruction attempt paths and the context-copy-before-recovery handler.
In main,158a tests U,158e tests the summary bits, and the unsafe edge1594→1749
runs FNCLEX then jumps beyond both transcendental calls. Exhausting64 flags ×
64 masks × four summary values gives14,197 allowed and2,187 skipped software
states. The guard addresses the identified synthetic-state risk; it does not
prove arbitrary spurious hardware faults impossible.

The remote H1672 directory contains only two public sources, ELF, disassembly
and compiler metadata. There are no inputs, freeze or hardware outputs. The
new guarded harness is unexecuted and has no compatible frozen campaign yet.
No host kernel, game service or unrelated process is modified.

H1668, H1669 preflight, H1671 software verification and H1672 static-report
directories reproduce byte-for-byte in
`/private/tmp/h1668-h1672-replay.K8uK1I`. No freezer or capture is repeated.
Original H1670 checksums and the remote hold/build hashes verify. All command
sessions are terminal; no capture or background process remains active.
Python/runner syntax, both emulator selftests, production build, diff and new-
file whitespace checks pass. These checks do not validate unseen silicon.

## Next action and verification

Create a fresh bank for the guarded62-field protocol, plus a pinned parser,
synthetic mutation preflight and independent scorer. Keep before-only skips
explicitly unobserved for FSIN/FCOS consumption. Use new significands and the
unchanged conservative freshness audit; do not silently move H1670's already
frozen inputs into a changed protocol. Any surviving unsafe states still need
a non-retrying trap observer outside the problematic Linux delivery path.
The guard is an instrumentation boundary, not a narrower definition of success.

Keep all small-normalization, zero/infinity, reserved-control, complete physical
tag/pointer, exact-center and all-input silicon obligations open. Canonical0339
source, empirical/incomplete R96, speculative-off defaults and documented
incumbent frontier direct50/48 and external81/79 remain unchanged. No new
hardware miss or successful hardware prediction has been obtained this turn.

## SHA256 anchors

- H1668 bank: `2730bb54fbaf1170b1c8b024edf12c1b340b1113613527c8c7df1a875d6c4511`.
- H1669 preflight: `b4463355082b6c1ea3fddc6e44d77b1f6b434433f013f648a5200dae20128b3f`.
- H1671 software preflight: `02d09d39506495366fe7c586653ef0b917e1df13ad551b50e5bb1320e3343920`.
- H1670 freeze: `7ef93a664008bcaee70cd536d4b98c633cdc75439f14fcf3915441c9d753d5f5`.
- H1670 manifest: `76cadb65ae1f78ea1dc00cc7fc70171645b0ee546b8f10830edc30a2a993868d`.
- H1670 hold: `5b7c5eabd952017949b4492132b19518cefc1e634db112d5c8e8c518d27dfdf2`.
- H1672 source: `9b5c6f81d666382c5d4acc9ec90a70a65531f09d955fbb921bb9b1edf30ae1c4`.
- H1672 ELF: `72d49ae21259b15c2941a19537976221459a731f5dc6d99d90de0e33e30e7bb6`.
- H1672 disassembly: `f9a2b4d37b96e58e4fae23c111c400d431e55d375cdd7bc850a75a523104d8ca`.
- H1672 static report: `e30681855b9fd394e03727f5d070b0b2a9835cff1a6186d44b19502e19aad7cd`.
- Linux v6.1 traps source: `da2b5ca41fbbec6594be05cdd9d6a246be81c85e859c1ef4fd967d30e43e659d`.
- Linux v6.1 FPU core source: `de01bb51a8963aa5cd8eebcdc229b40e98fb0682e6b0f93cba82c973bf05e9d8`.
