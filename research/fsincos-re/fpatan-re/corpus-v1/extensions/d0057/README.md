# D0057: long even-inner RN64 tie discrimination

This immutable input-only extension contains 2,616 fresh capture tuples:
nine exact external endpoint witnesses, all 3x3 both-operand neighborhoods,
eight sign/octant transformations, all four rounding modes and sampled
PC24/53 groups. The separate nine-row input pool is construction metadata,
not another capture-protocol pack. Neither file contains hardware labels,
model predictions or private history.

One-shot Skylake capture completed on CPUID `00050654`, microcode `0x1`.
The unchanged main program has zero output/C1/exception/pre-load misses.
At the long even-inner addition, nearest/odd fails on 36 union rows,
ties-away on two and ties-zero on 34; nearest/even matches every row.
Both retained parities discriminate. This identifies among those fixed
rules within the frozen operation graph, not arbitrary state-dependent
rules or all CPUs. All twelve three-PC groups agree.

Use [CATALOG-D0057.json](../../CATALOG-D0057.json) for the complete
7,122,264-tuple, twenty-pack corpus. D0057 has not yet run on i7. Never
repeat these observed tuples on the recorded Skylake context; reuse their
saved observations. Audit the destination tuple history before capturing
on another context. Original packs and catalogs remain unchanged.
